spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWRD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#  servlet:
#    multipart:
#      enabled: true
#      max-file-size: 5MB
#      max-request-size: 10MB
#      file-size-threshold: 2KB
#  minio:
#    url: http://localhost:9000
##    bucket: app-bucket
#    access-key: eorWu876q3r5nU7bsXwx
#    secret-key: Yy7Yb1zpS93rypjIek2k8COsEuS5Ryy3KmC0yFWF
    create-bucket: true
jwt:
  secret_key: ${JWT_SECRET_KEY}
springdoc:
  api-docs:
    path: /api/v1/docs
  swagger-ui:
    path: /api/v1/docs/
logging:
  level:
    sql: debug
    web: trace
    root: trace
  file:
    name: main.log
  logback:
    rollingpolicy:
      max-file-size: 10MB